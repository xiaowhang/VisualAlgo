import { bstSearchRegistry } from '@/algorithms/definitions/trees/bst-search.registry';
import type { AlgorithmDefinition } from '@/types/algorithm';

export const treesRegistries: AlgorithmDefinition[] = [bstSearchRegistry];
